// __tests__/authController.test.js
const authController = require('../controllers/authController');

describe('Dummy test for SonarCloud', () => {
  it('should exist', () => {
    expect(authController).toBeDefined();
  });
});
